# VoxShift 技術設計書

**文書バージョン:** 1.0 Final
**対象バージョン:** VoxShift 1.0
**作成日:** 2026年7月24日
**対象OS:** Windows 10 / Windows 11 x64
**開発言語:** Rust
**GUI:** Slint
**Discord連携:** Discord RPC
**VRChat連携:** OSC

---

# 1. プロジェクト概要

VoxShiftは、VRChatとDiscordのマイク・セルフミュート状態を連動させるWindows常駐アプリケーションである。

VRChatとDiscordの両方へ同時に音声が送信されることを防ぎ、ユーザーが現在使用したい音声先を、マイク操作だけで自然に切り替えられるようにする。

VoxShift独自のアカウント、ログイン画面、ユーザー登録、クラウド設定同期は実装しない。

Discord RPCを利用するため、初回のみDiscordクライアント上でVoxShiftへのアクセスを許可する必要がある。

---

# 2. 確定技術構成

| 項目        | 採用技術                         |
| --------- | ---------------------------- |
| 開発言語      | Rust Stable / Edition 2024   |
| GUI       | Slint 1.17以降                 |
| 非同期処理     | Tokio current-thread runtime |
| VRChat連携  | OSC over UDP                 |
| Discord連携 | Discord RPC over Named Pipe  |
| 設定保存      | JSON                         |
| 認証情報保存    | Windows Credential Manager   |
| ログ        | tracing                      |
| 対象アーキテクチャ | x86_64-pc-windows-msvc       |
| アプリ形態     | タスクトレイ常駐                     |
| 管理者権限     | 不要                           |
| 独自ログイン    | なし                           |
| 外部データベース  | なし                           |
| テレメトリー    | なし                           |

Rustはランタイムやガベージコレクターを必要とせず、メモリ効率とスレッド安全性を重視した言語であるため、軽量な常駐アプリに適している。

---

# 3. 用語定義

本設計書では、以下の表現を使用する。

| 表現             | 内部状態                    |
| -------------- | ----------------------- |
| VRChatマイクオン    | `vrchat_muted = false`  |
| VRChatマイクオフ    | `vrchat_muted = true`   |
| Discord VCミュート | `discord_muted = true`  |
| Discord VCオン   | `discord_muted = false` |

ここでいうDiscord VCミュートは、Discordのセルフミュート状態を指す。サーバーミュートや他ユーザーから設定されたミュートは対象外とする。

---

# 4. 動作モード

VoxShiftには、次の2つの連動モードを実装する。

## 4.1 反転連動モード

VRChatとDiscordを双方向に連動させる標準モードである。

### 動作表

| 操作・状態変化           | VoxShiftの処理       |
| ----------------- | ----------------- |
| VRChatマイクをオン      | DiscordをVCミュート    |
| VRChatマイクをオフ      | DiscordのVCミュートを解除 |
| DiscordをVCミュート    | VRChatマイクをオン      |
| DiscordのVCミュートを解除 | VRChatマイクをオフ      |

維持する関係は次のとおりである。

```text
DiscordMuted = !VrChatMuted
```

正常な状態の組み合わせは次の2種類だけとなる。

| VRChat | Discord |
| ------ | ------- |
| マイクオン  | VCミュート  |
| マイクオフ  | VCオン    |

## 4.2 VRChat優先モード

VRChatからDiscordへの片方向連動だけを行う。

### 動作表

| 操作・状態変化           | VoxShiftの処理       |
| ----------------- | ----------------- |
| VRChatマイクをオン      | DiscordをVCミュート    |
| VRChatマイクをオフ      | DiscordのVCミュートを解除 |
| DiscordをVCミュート    | VRChatを変更しない      |
| DiscordのVCミュートを解除 | VRChatを変更しない      |

Discord側で行われた操作は、VRChatへ反映しない。

また、Discordで手動操作が行われた直後に、VoxShiftがDiscordを元の状態へ戻す処理も行わない。次にVRChatのマイク状態が変化したとき、またはユーザーが「再同期」を実行したときにDiscordへ反映する。

この方式により、Discordは必要に応じて独立して操作できる。

## 4.3 一時停止

連動処理を一時停止できる。

一時停止中もVRChatとDiscordの状態監視は継続するが、どちらにも変更命令を送信しない。

一時停止を解除した際は、設定に応じて次のどちらかを実行する。

* 現在の状態を維持して次の操作を待つ
* VRChatを基準に即時再同期する

初期設定は「VRChatを基準に即時再同期」とする。

---

# 5. 機能範囲

## 5.1 実装対象

* VRChatマイク状態の取得
* VRChatマイクのオン・オフ操作
* Discordセルフミュート状態の取得
* Discordセルフミュートの設定
* Discord VC接続状態の取得
* 双方向の反転連動
* VRChatからDiscordへの片方向連動
* 連動の一時停止
* 手動再同期
* Discord初回認可
* Discordトークンの安全な保存
* Discord RPC自動再接続
* VRChat OSC再接続
* Windowsログオン時の自動起動
* タスクトレイ常駐
* 診断ログ表示
* ダーク・ライト・システムテーマ
* エラー通知

## 5.2 実装対象外

* Discord Botの導入
* Discordサーバーへの参加
* Discord Gatewayへの接続
* Discord Web版への対応
* Discordモバイル版への対応
* VRChat APIへのログイン
* VRChatアカウント情報の取得
* 音声データの録音・解析
* 仮想オーディオデバイスの作成
* マイクデバイス自体の有効・無効化
* サーバーミュートの解除
* 他ユーザーの音声設定変更
* macOSおよびLinux対応
* 設定のクラウド同期
* 利用状況テレメトリー

---

# 6. 外部連携仕様

# 6.1 Discord RPC

## 6.1.1 接続方式

Windows版DiscordデスクトップクライアントのローカルNamed Pipeへ接続する。

探索対象は次のとおりである。

```text
\\?\pipe\discord-ipc-0
\\?\pipe\discord-ipc-1
...
\\?\pipe\discord-ipc-9
```

接続後、RPCバージョンとDiscord Application Client IDを含むハンドシェイクを送信する。Discord公式仕様ではRPCバージョン1が現行で、正常に接続すると`READY`イベントが返される。

## 6.1.2 使用するRPCコマンド

| コマンド                         | 用途                 |
| ---------------------------- | ------------------ |
| `AUTHORIZE`                  | 初回認可               |
| `AUTHENTICATE`               | 保存済みアクセストークンによる認証  |
| `GET_VOICE_SETTINGS`         | Discordセルフミュート状態取得 |
| `SET_VOICE_SETTINGS`         | Discordセルフミュート状態変更 |
| `GET_SELECTED_VOICE_CHANNEL` | VC参加状態取得           |
| `SUBSCRIBE`                  | イベント購読             |
| `UNSUBSCRIBE`                | イベント購読解除           |

`GET_VOICE_SETTINGS`のレスポンスにはセルフミュートを示す`mute`フィールドが含まれる。`SET_VOICE_SETTINGS`は指定したフィールドだけを変更できるため、VoxShiftは`mute`以外の入力デバイス、音量、ノイズ抑制などを変更しない。

ミュート操作例は次の形となる。

```json
{
  "cmd": "SET_VOICE_SETTINGS",
  "args": {
    "mute": true
  },
  "nonce": "generated-uuid"
}
```

## 6.1.3 使用するRPCイベント

| イベント                      | 用途          |
| ------------------------- | ----------- |
| `VOICE_SETTINGS_UPDATE`   | ミュート状態変更の検出 |
| `VOICE_CHANNEL_SELECT`    | VC参加・退出の検出  |
| `VOICE_CONNECTION_STATUS` | VC接続状態の監視   |
| `ERROR`                   | RPCエラー検出    |
| `READY`                   | 接続完了検出      |

Discordは`VOICE_SETTINGS_UPDATE`を、クライアントの音声設定が変更された際のイベントとして提供している。

イベント受信後は、イベントデータだけで確定せず、必要に応じて`GET_VOICE_SETTINGS`を実行して最新状態を取得する。

## 6.1.4 Discord側の重要な制約

Discord RPCでは、同時に音声設定を変更できるアプリは1つだけである。

VoxShiftが音声設定を変更すると、他のRPCアプリは音声設定を変更できなくなる可能性がある。また、VoxShiftがDiscord RPCから切断された場合、Discordの音声設定はVoxShift接続前の状態へ戻ることがある。再接続時には、以前VoxShiftが設定した音声状態がDiscord側で再適用される場合もある。

そのため、VoxShiftはDiscord再接続後に必ず状態を再取得し、VRChatを基準として再同期する。

---

# 6.2 Discord認可

## 6.2.1 アカウント方針

VoxShift独自のアカウント登録は実装しない。

必要となるのは、Discord RPCを使用するためのDiscord認可だけである。

初回利用時は次の処理を行う。

```text
VoxShift起動
  ↓
Discord RPCへ接続
  ↓
Discordクライアントに認可画面を表示
  ↓
ユーザーがVoxShiftを許可
  ↓
認可コードを取得
  ↓
Token Brokerでアクセストークンへ交換
  ↓
Windows Credential Managerへ保存
```

要求するOAuth2スコープは次の2つだけとする。

```text
rpc
identify
```

Discord RPCの`AUTHORIZE`コマンドはDiscordクライアント上に認可画面を表示し、認可後にOAuth2認可コードを返す。取得したコードはアクセストークンへ交換し、`AUTHENTICATE`コマンドへ渡す必要がある。

## 6.2.2 Token Broker

Discord Client SecretをVoxShiftの実行ファイルへ埋め込んではならない。

そのため、公開版では最小構成のToken Brokerを用意する。

```text
┌──────────────┐       HTTPS       ┌────────────────────┐
│   VoxShift   │ ────────────────▶ │ VoxShift Token     │
│ Desktop App  │  authorization    │ Broker             │
│              │  code / refresh   │                    │
└──────────────┘                   └─────────┬──────────┘
                                            │
                                            ▼
                                  Discord OAuth2 Endpoint
```

Token Brokerの役割は以下だけとする。

* 認可コードをDiscordアクセストークンへ交換
* リフレッシュトークンを使用してトークンを更新
* 認可解除時にトークンを失効
* Client Secretの安全な保持

Token Brokerは次の情報を保存しない。

* DiscordユーザーID
* Discordユーザー名
* アクセストークン
* リフレッシュトークン
* 認可コード
* IPアドレスを含む恒久的なアクセス履歴

OAuth2の認可コード交換にはClient Secretが必要となるため、公開デスクトップアプリへSecretを埋め込まず、サーバー側で交換する構成とする。DiscordもOAuth2アクセストークンをパスワード同様に扱い、漏えいさせないよう求めている。

## 6.2.3 Token Broker実装

| 項目            | 設計                    |
| ------------- | --------------------- |
| 言語            | Rust                  |
| Webフレームワーク    | Axum                  |
| 通信            | HTTPSのみ               |
| データベース        | なし                    |
| セッション         | なし                    |
| Client Secret | ホスティング環境のSecret Store |
| ログ            | エラーコードのみ              |
| レート制限         | IPおよびインストールID単位       |
| タイムアウト        | 10秒                   |
| リクエスト最大サイズ    | 16KB                  |

エンドポイントは次の3つに限定する。

```text
POST /v1/discord/exchange
POST /v1/discord/refresh
POST /v1/discord/revoke
```

## 6.2.4 トークン保存

クライアント側ではWindows Credential Managerを使用する。

Credential Target名は次のとおりとする。

```text
VoxShift/DiscordOAuth
```

保存対象：

* Access Token
* Refresh Token
* Expiration Time
* Authorized Scopes

以下は`config.json`やログへ保存しない。

* Access Token
* Refresh Token
* Client Secret
* Authorization Code
* RPC Token

アクセストークンの有効期限5分前に自動更新する。

更新に失敗した場合は現在のアクセストークンを有効期限まで使用し、期限切れ後にDiscord再認可を要求する。

## 6.2.5 Discord公開申請

開発中の未承認Discordアプリは、Developer Portalで登録されたテスターだけがRPCを利用でき、テスター枠は最大50名である。一般公開にはDiscord側の承認が必要となる。

したがって、VoxShiftの一般公開条件に次を含める。

* Discord Developer Application作成
* RPC機能の申請
* プライバシーポリシーの公開
* 利用規約の公開
* Discordによるアプリ承認
* 本番用Token Brokerの稼働

Discordから承認されるまでは、VoxShiftを開発版またはクローズドテスト版として扱う。

---

# 6.3 VRChat OSC

## 6.3.1 使用ポート

初期値は次のとおりとする。

| 方向                | アドレス        |  ポート |
| ----------------- | ----------- | ---: |
| VoxShift → VRChat | `127.0.0.1` | 9000 |
| VRChat → VoxShift | `127.0.0.1` | 9001 |

ポート番号は設定画面から変更可能とする。

受信元は初期状態でループバックアドレスだけに制限する。

## 6.3.2 VRChat状態取得

次のOSCアドレスを監視する。

```text
/avatar/parameters/MuteSelf
```

`MuteSelf`はVRChatの組み込みBoolパラメーターで、ユーザー自身がミュート中なら`true`、マイクがオンなら`false`を返す。

受信値は次のように変換する。

```text
MuteSelf = true  → VRChatマイクオフ
MuteSelf = false → VRChatマイクオン
```

## 6.3.3 VRChatマイク操作

次のOSC入力を使用する。

```text
/input/Voice
```

VRChatの「Toggle Voice」が有効な場合、`0`から`1`への変化によってマイク状態が反転する。その後`0`へ戻す必要がある。

送信シーケンスは次のとおりとする。

```text
/input/Voice 1
60ms待機
/input/Voice 0
```

## 6.3.4 必須設定

利用者はVRChatで次を有効にする必要がある。

```text
OSC: Enabled
Toggle Voice: Enabled
```

「Toggle Voice」が無効な場合、`/input/Voice`はPush-To-Mute動作になるため、VoxShift 1.0では正式対応外とする。

## 6.3.5 OSCアバター設定

VRChatはアバターごとにOSC設定ファイルを生成し、設定された出力パラメーターを値の変化時に外部アプリへ送信する。既存のOSC設定で`MuteSelf`出力が削除されている場合、VoxShiftは状態を取得できない。

VoxShiftは現在のアバター変更を次のアドレスで検出する。

```text
/avatar/change
```

アバター変更時には次を実行する。

1. VRChat状態を`Unknown`へ変更
2. 新しい`MuteSelf`の受信を待機
3. OSC設定ファイルの存在を診断
4. 3秒以内に取得できなければ警告を表示

VoxShiftはOSC設定ファイルを無断で変更しない。

診断画面に「OSC設定を修復」ボタンを用意し、ユーザーが明示的に実行した場合だけ次を行う。

1. 元ファイルをバックアップ
2. `MuteSelf`出力設定を追加
3. 変更内容を表示
4. VRChatまたはアバターの再読み込みを案内

## 6.3.6 初期状態取得

VoxShift起動後に`MuteSelf`が送信されない場合、現在のVRChat状態を推測しない。

画面に次を表示する。

```text
VRChatマイク状態を待機しています
マイクを一度切り替えて同期してください
```

状態確認のためにVoxShiftが自動的にマイクを反転する処理は行わない。

---

# 7. GUI設計

# 7.1 GUIフレームワーク

GUIにはSlintを採用する。

SlintはRustで実装された宣言型GUIツールキットで、UIをネイティブコードへコンパイルできる。軽量なランタイムとGPUアクセラレーションに対応しており、Slint 1.17ではWindowsを含むシステムトレイ機能も正式に追加されている。

WebView、HTML、CSS、JavaScriptは使用しない。

## 7.2 デザインコンセプト

デザインテーマは次のとおりとする。

```text
Dual Voice Bridge
```

VRChatとDiscordの2つの音声経路が、中央のリンクを通じて切り替わる様子を視覚化する。

### 基本色

| 要素      | 色の方向性     |
| ------- | --------- |
| 背景      | 深いネイビーから黒 |
| VRChat  | シアン・青緑    |
| Discord | ブルーパープル   |
| 正常      | グリーン      |
| 一時停止    | アンバー      |
| エラー     | レッド       |
| 無効      | グレー       |

色だけで状態を表さず、必ずアイコンと文字を併用する。

## 7.3 メインウィンドウ

推奨サイズ：

```text
820 × 560 px
```

最小サイズ：

```text
720 × 500 px
```

画面構成：

```text
┌────────────────────────────────────────────────────────┐
│ VoxShift                                     ─ □ ×    │
├────────────┬───────────────────────────────────────────┤
│ Dashboard  │                                           │
│ Link Mode  │   ┌──────────┐       ┌──────────┐        │
│ Settings   │   │ VRChat   │◀─────▶│ Discord  │        │
│ Diagnostics│   │ Mic ON   │       │ Muted    │        │
│ About      │   └──────────┘       └──────────┘        │
│            │                                           │
│            │       [ 反転連動 ] [ 一時停止 ]           │
│            │                                           │
│            │   Last sync: 17:24:31                     │
└────────────┴───────────────────────────────────────────┘
```

## 7.4 ダッシュボード

ダッシュボードには次を表示する。

### VRChatカード

* OSC接続状態
* マイク状態
* 現在のアバター検出状態
* 最終状態更新時刻
* OSCポート
* 接続診断ボタン

### Discordカード

* RPC接続状態
* 認可状態
* VC参加状態
* セルフミュート状態
* 最終状態更新時刻
* 再認可ボタン

### 中央リンク

中央には現在の連動方向を示す。

反転連動：

```text
VRChat ⇄ Discord
```

VRChat優先：

```text
VRChat → Discord
```

一時停止：

```text
VRChat ╳ Discord
```

状態変化時にのみ、リンク上へ短いパルスアニメーションを表示する。

常時アニメーションは実行せず、バックグラウンド時のGPU・CPU使用を抑える。

## 7.5 ウィンドウ装飾

* 角丸カード
* 細いグラデーション境界線
* 控えめな影
* 状態変化時の200msフェード
* 接続中の短いリングアニメーション
* Windows 11ではMica背景を任意で使用
* Windows 10では不透明な背景へフォールバック
* 「視覚効果を減らす」設定を用意

## 7.6 タスクトレイ

Slintの`SystemTrayIcon`を使用する。Windowsではシステム通知領域のネイティブアイコンとして実装される。

トレイメニュー：

```text
VoxShift
────────────────
● 連動中
  一時停止
────────────────
● 反転連動
○ VRChat優先
────────────────
VRChat: マイクオン
Discord: VCミュート
────────────────
再同期
設定を開く
診断情報
終了
```

トレイアイコンの左クリックでメインウィンドウを表示する。

ウィンドウの閉じるボタンではアプリを終了せず、タスクトレイへ格納する。

## 7.7 トレイアイコン状態

| 状態           | アイコン       |
| ------------ | ---------- |
| 正常連動         | 通常         |
| VRChatマイクオン  | VRChat側強調  |
| Discord VCオン | Discord側強調 |
| 一時停止         | Pauseバッジ   |
| 接続待機         | 黄色ドット      |
| エラー          | 赤い警告バッジ    |

## 7.8 アクセシビリティ

* キーボードだけで操作可能
* タブ移動順を定義
* 状態を色だけで判別させない
* 最小コントラスト比を確保
* OSの表示倍率に対応
* アニメーション無効化設定
* スクリーンリーダー向けラベル
* フォントサイズ変更に対応

---

# 8. システム構成

```text
┌───────────────────────────────────────────────────────┐
│                     VoxShift                          │
│                                                       │
│  ┌─────────────────────────────────────────────────┐ │
│  │ Slint UI / System Tray                          │ │
│  └──────────────────────┬──────────────────────────┘ │
│                         │ UI Command / Snapshot       │
│  ┌──────────────────────▼──────────────────────────┐ │
│  │ Voice Coordinator                              │ │
│  │ 状態管理・競合解決・連動ルール・再同期          │ │
│  └───────────────┬────────────────┬────────────────┘ │
│                  │                │                  │
│  ┌───────────────▼──────┐ ┌───────▼────────────────┐ │
│  │ VRChat OSC Adapter   │ │ Discord RPC Adapter    │ │
│  │ UDP 9000 / 9001      │ │ Named Pipe / OAuth2    │ │
│  └──────────────────────┘ └────────────┬───────────┘ │
│                                        │             │
│                              ┌─────────▼──────────┐  │
│                              │ Token Broker       │  │
│                              │ HTTPS / Stateless  │  │
│                              └────────────────────┘  │
└───────────────────────────────────────────────────────┘
```

---

# 9. スレッド・非同期設計

## 9.1 UIスレッド

Slintのイベントループをメインスレッドで実行する。

UIスレッドでは以下を行わない。

* UDP待機
* Named Pipe待機
* HTTP通信
* JSONファイル書き込み
* 再接続待機
* 長時間のトークン処理

## 9.2 ワーカースレッド

バックグラウンドに1本のワーカースレッドを作成し、Tokioのcurrent-thread runtimeを実行する。

担当処理：

* Discord Named Pipe
* Discord RPCフレーム処理
* VRChat OSC受信
* VRChat OSC送信
* Token Broker通信
* 再接続処理
* 状態調停
* ログ書き込み

通常時に複数のTokioワーカースレッドを起動しない。

## 9.3 チャンネル

```rust
tokio::sync::mpsc::channel::<CoordinatorCommand>(64);
tokio::sync::watch::channel::<AppSnapshot>(initial_snapshot);
```

UIからの操作はMPSCチャンネルへ送信する。

状態表示は`watch`チャンネルの最新スナップショットをSlint UIへ反映する。

UI更新には`slint::invoke_from_event_loop`を使用する。

---

# 10. 内部状態モデル

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum MuteState {
    Unknown,
    Muted,
    Unmuted,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ConnectionState {
    Disconnected,
    Connecting,
    Authorizing,
    Connected,
    Degraded,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum LinkMode {
    InverseBidirectional,
    VrChatMaster,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum LinkState {
    Active,
    Paused,
    WaitingForState,
    Faulted,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum EventOrigin {
    VrChat,
    Discord,
    Coordinator,
    Startup,
    Reconnect,
    UserInterface,
}
```

状態スナップショット：

```rust
pub struct AppSnapshot {
    pub vrchat_connection: ConnectionState,
    pub vrchat_mute: MuteState,

    pub discord_connection: ConnectionState,
    pub discord_mute: MuteState,
    pub discord_in_voice_channel: bool,

    pub link_mode: LinkMode,
    pub link_state: LinkState,

    pub last_sync_at: Option<std::time::SystemTime>,
    pub last_error: Option<String>,
}
```

---

# 11. 状態調停アルゴリズム

## 11.1 イベント直列化

VRChatとDiscordの状態イベントは、すべてVoice Coordinatorへ投入する。

Voice Coordinatorだけが状態を変更し、アダプターへ命令を送る。

これにより次を防止する。

* 同時操作による競合
* 二重トグル
* 無限反転
* 古い状態による上書き
* UIとバックグラウンド処理の競合

## 11.2 VRChatからの状態変更

```text
MuteSelf受信
  ↓
VRChat状態更新
  ↓
一時停止中か確認
  ↓
Discordの目標状態を算出
  ↓
Discordが目標状態と異なる場合だけSET_VOICE_SETTINGS
```

疑似コード：

```rust
async fn handle_vrchat_state(&mut self, state: MuteState) {
    self.vrchat_state = state;

    if self.link_state != LinkState::Active {
        return;
    }

    let target = match state {
        MuteState::Muted => MuteState::Unmuted,
        MuteState::Unmuted => MuteState::Muted,
        MuteState::Unknown => return,
    };

    if self.discord_state != target {
        self.set_discord_state(target).await;
    }
}
```

## 11.3 Discordからの状態変更

```text
VOICE_SETTINGS_UPDATE
  ↓
最新設定をGET_VOICE_SETTINGSで取得
  ↓
VoxShift自身の操作に対する応答か判定
  ↓
反転連動モードならVRChat目標状態を算出
  ↓
現在状態と異なる場合だけ/input/Voiceを送信
```

VRChat優先モードでは、Discordの状態表示だけを更新し、VRChatを変更しない。

## 11.4 自己操作イベントの識別

VoxShiftがDiscordへ設定した直後には、Discordから同じ状態変更イベントが返る。

これをユーザー操作として再処理しないため、次を保持する。

```rust
pub struct PendingDiscordCommand {
    pub nonce: uuid::Uuid,
    pub target: MuteState,
    pub issued_at: std::time::Instant,
    pub deadline: std::time::Instant,
}
```

Discord RPCレスポンスの`nonce`と目標状態が一致した場合、VoxShift自身の操作への確認応答として処理する。

VRChat側にはコマンドIDがないため、次を保持する。

```rust
pub struct PendingVrChatCommand {
    pub previous: MuteState,
    pub expected: MuteState,
    pub issued_at: std::time::Instant,
    pub deadline: std::time::Instant,
}
```

`MuteSelf`が期待値と一致すれば操作成功とする。

## 11.5 VRChatトグルの安全対策

VRChatは絶対値設定ではなくトグル操作となるため、現在状態が`Unknown`の場合は操作しない。

```text
現在状態が不明
  → /input/Voiceを送信しない
```

送信後1.5秒以内に期待する`MuteSelf`を取得できなかった場合は、再度トグルを送信しない。

再送すると、最初の操作が実際には成功していた場合に元へ戻してしまう可能性があるためである。

この場合は次を実行する。

* VRChat状態を`Unknown`へ変更
* 連動状態を`Degraded`へ変更
* Discordへの追加操作を停止
* ユーザーへ再同期を要求

## 11.6 競合時の優先ルール

ユーザーがVRChatとDiscordをほぼ同時に操作した場合は、最後に外部から確認された操作を優先する。

判定にはVoice Coordinator内の単調増加シーケンスを使用する。

```rust
pub struct VoiceEvent {
    pub sequence: u64,
    pub origin: EventOrigin,
    pub state: MuteState,
    pub received_at: std::time::Instant,
}
```

VoxShift自身が発行した命令より後に、異なる外部状態が届いた場合はユーザー操作として扱い、保留中の命令を破棄する。

---

# 12. 起動・再同期仕様

## 12.1 起動順序

```text
設定読み込み
  ↓
タスクトレイ作成
  ↓
OSCソケット開始
  ↓
Discord Named Pipe探索
  ↓
Discord認証
  ↓
Discord状態取得
  ↓
VRChat状態待機
  ↓
両方の状態確定
  ↓
初期同期
```

## 12.2 初期同期の優先元

初期設定ではVRChatを優先する。

起動時に状態が次のようになっていた場合：

```text
VRChatマイクオン
Discord VCオン
```

Discordをミュートし、VRChatは変更しない。

設定画面では次を選択可能とする。

* VRChatを優先
* Discordを優先
* 起動時は変更しない

ただし、VRChat優先モードでは常にVRChatを優先する。

## 12.3 手動再同期

「再同期」では次を実行する。

1. Discord状態を再取得
2. VRChat状態が既知か確認
3. 保留中のコマンドを破棄
4. 優先元に従って状態を統一
5. 結果をUIへ表示

VRChat状態が不明な場合は、OSCトグルを送信せず、マイクを一度手動で切り替えるよう案内する。

---

# 13. 切断・復旧処理

## 13.1 Discord再接続

Discord Named Pipe切断時：

1. Discord状態を`Unknown`へ変更
2. VRChatを変更しない
3. RPC制御を停止
4. 再接続を開始
5. 再認証
6. 音声状態を再取得
7. VRChatを基準に再同期

再接続間隔：

```text
1秒 → 2秒 → 5秒 → 10秒 → 15秒 → 30秒
```

最大30秒で固定する。

Discordプロセスを常時短周期でポーリングせず、Named Pipe接続失敗時だけ再試行する。

## 13.2 VRChat切断

OSCはUDPであるため、ソケット接続状態だけではVRChat起動状態を判定できない。

次の条件でVRChatを切断扱いにする。

* `/avatar/change`または`MuteSelf`を一度も受信していない
* VRChatプロセスが存在しない
* OSC送信後に期待状態を確認できない
* Windowsスリープ復帰後に状態確認できない

VRChatのプロセス検出は補助情報として使用し、マイク状態の推測には使用しない。

## 13.3 スリープ復帰

Windowsスリープ復帰時：

* Discord Named Pipeを再接続
* OAuth2トークン期限を確認
* OSCソケットを再作成
* VRChat状態を`Unknown`へ変更
* 両方の状態が確定するまで操作を保留
* 初期同期ルールで再同期

---

# 14. 設定保存

保存先：

```text
%LOCALAPPDATA%\VoxShift\config.json
```

設定例：

```json
{
  "schemaVersion": 1,
  "linkMode": "inverseBidirectional",
  "linkEnabled": true,
  "startupAuthority": "vrchat",
  "resumePolicy": "syncFromVrchat",
  "theme": "system",
  "reducedMotion": false,
  "vrchat": {
    "host": "127.0.0.1",
    "sendPort": 9000,
    "receivePort": 9001,
    "commandTimeoutMs": 1500
  },
  "discord": {
    "clientId": "VOXSHIFT_DISCORD_CLIENT_ID",
    "connectAutomatically": true
  },
  "startup": {
    "runWithWindows": false,
    "startMinimized": true
  },
  "logging": {
    "level": "warning",
    "fileLogging": false
  }
}
```

認証情報は含めない。

保存時は一時ファイルを使用する。

```text
config.json.tmpへ書き込み
  ↓
Flush
  ↓
既存config.jsonと置換
```

破損した設定はバックアップし、初期値で起動する。

---

# 15. ログ設計

通常時はメモリ上に最大500件のリングバッファを保持する。

例：

```text
17:24:31.104 INFO  VRChat MuteSelf=false
17:24:31.105 DEBUG Desired Discord mute=true
17:24:31.106 INFO  Discord SET_VOICE_SETTINGS sent
17:24:31.142 INFO  Discord mute=true confirmed
17:24:31.143 INFO  Synchronization completed
```

ファイルログは初期状態で無効とする。

有効化時の保存先：

```text
%LOCALAPPDATA%\VoxShift\logs\
```

ローテーション：

| 項目      |   値 |
| ------- | --: |
| 1ファイル最大 | 1MB |
| 最大ファイル数 |   5 |
| 合計最大    | 5MB |

以下をログへ出力しない。

* Access Token
* Refresh Token
* Client Secret
* Authorization Code
* Discordメールアドレス
* DiscordユーザーID
* VRChatユーザーID
* アバターIDの全体
* Token Brokerリクエスト本文

診断用アバターIDは末尾8文字だけを表示する。

---

# 16. セキュリティ・プライバシー

* VoxShift独自アカウントを作成しない
* VRChat APIへログインしない
* Discord Botを使用しない
* Client Secretを実行ファイルへ含めない
* DiscordトークンをWindows Credential Managerへ保存
* OSCは初期状態でループバック通信だけを許可
* Token BrokerはHTTPSのみ
* Token Brokerにデータベースを設置しない
* トークンや認可コードをサーバーログへ残さない
* テレメトリーを送信しない
* クラッシュログを自動送信しない
* 診断情報はユーザー操作時だけクリップボードへ出力
* RPCフレームサイズに1MBの上限を設ける
* OSCパケットサイズに64KBの上限を設ける
* 設定ファイルサイズに256KBの上限を設ける
* 不正JSONや不正OSCパケットでプロセスを終了させない
* 配布EXEとインストーラーへコード署名を付与する

---

# 17. パフォーマンス目標

| 項目                  |      目標 |
| ------------------- | ------: |
| アイドル時CPU            |  0.3%未満 |
| 常駐時メモリ              |  50MB以下 |
| ウィンドウ非表示時GPU        |    ほぼ0% |
| 起動時間                |    1秒以内 |
| VRChatからDiscordへの反映 | 300ms以内 |
| DiscordからVRChatへの反映 | 500ms以内 |
| 配布サイズ               |  20MB以下 |
| バックグラウンドスレッド        |    原則1本 |
| 常時ネットワーク通信          |      なし |

ネットワーク通信が発生するのは次の場合だけとする。

* Discord初回認可
* OAuth2トークン更新
* Discord認可解除
* 任意のアップデート確認

VRChatとDiscordの通常連動はローカル通信だけで完結する。

---

# 18. Cargoワークスペース構成

```text
VoxShift/
├─ Cargo.toml
├─ Cargo.lock
├─ rust-toolchain.toml
│
├─ crates/
│  ├─ voxshift-app/
│  │  ├─ src/main.rs
│  │  ├─ src/application.rs
│  │  └─ build.rs
│  │
│  ├─ voxshift-core/
│  │  ├─ src/coordinator.rs
│  │  ├─ src/state.rs
│  │  ├─ src/event.rs
│  │  ├─ src/command.rs
│  │  └─ src/error.rs
│  │
│  ├─ voxshift-discord/
│  │  ├─ src/ipc.rs
│  │  ├─ src/frame.rs
│  │  ├─ src/client.rs
│  │  ├─ src/auth.rs
│  │  ├─ src/voice.rs
│  │  └─ src/models.rs
│  │
│  ├─ voxshift-vrchat/
│  │  ├─ src/osc_receiver.rs
│  │  ├─ src/osc_sender.rs
│  │  ├─ src/avatar_config.rs
│  │  └─ src/process.rs
│  │
│  ├─ voxshift-platform-windows/
│  │  ├─ src/credentials.rs
│  │  ├─ src/startup.rs
│  │  ├─ src/single_instance.rs
│  │  ├─ src/power_events.rs
│  │  └─ src/dwm.rs
│  │
│  ├─ voxshift-storage/
│  │  ├─ src/config.rs
│  │  └─ src/logging.rs
│  │
│  └─ voxshift-ui/
│     ├─ ui/main-window.slint
│     ├─ ui/dashboard.slint
│     ├─ ui/settings.slint
│     ├─ ui/diagnostics.slint
│     ├─ ui/components/
│     └─ assets/
│
├─ services/
│  └─ token-broker/
│     ├─ Cargo.toml
│     └─ src/main.rs
│
└─ tests/
   ├─ coordinator-tests/
   ├─ discord-mock/
   ├─ osc-mock/
   └─ end-to-end/
```

---

# 19. 主要依存クレート

| クレート               | 用途             |
| ------------------ | -------------- |
| `slint`            | GUI            |
| `slint-build`      | UIコンパイル        |
| `tokio`            | 非同期ランタイム       |
| `serde`            | データ変換          |
| `serde_json`       | JSON           |
| `rosc`             | OSC            |
| `reqwest`          | Token Broker通信 |
| `rustls`           | TLS            |
| `windows`          | Windows API    |
| `tracing`          | ログ             |
| `tracing-appender` | ファイルログ         |
| `uuid`             | RPC nonce      |
| `thiserror`        | エラー定義          |
| `secrecy`          | トークンの安全な保持     |
| `zeroize`          | 機密情報のメモリ消去     |
| `directories`      | 保存先取得          |

依存クレートのバージョンは`Cargo.lock`で固定し、自動更新による互換性破壊を避ける。

Discord RPC部分は、用途がRich Presenceだけに限定された既存クレートへ依存せず、公式RPCフレーム仕様に基づいてVoxShift内で実装する。

---

# 20. 多重起動防止

Windows名前付きMutexを使用する。

```text
Local\VoxShift.SingleInstance
```

2つ目のVoxShiftが起動された場合：

1. 既存プロセスへウィンドウ表示要求を送る
2. 新しいプロセスを終了する

プロセス間通信には次のNamed Pipeを使用する。

```text
\\.\pipe\VoxShift.Control
```

---

# 21. Windows自動起動

ユーザーが設定を有効にした場合だけ、現在のユーザー向け自動起動を登録する。

管理者権限は要求しない。

自動起動時の引数：

```text
VoxShift.exe --background
```

`--background`指定時はメインウィンドウを表示せず、タスクトレイへ常駐する。

---

# 22. エラー処理

| エラー                          | 処理                   |
| ---------------------------- | -------------------- |
| Discordが未起動                  | 再接続待機                |
| Discord RPC Pipeが見つからない      | バックオフ再試行             |
| Discord未認可                   | 認可画面を表示              |
| Access Token期限切れ             | Refresh Tokenで更新     |
| Refresh Token無効              | 再認可を要求               |
| Discord RPC承認対象外             | テスター登録またはアプリ承認を案内    |
| Discord音声設定がロック中             | 競合アプリ名が不明であることを示して停止 |
| Discord VC未接続                | 状態を表示し、目標状態を保持       |
| OSCポート使用中                    | 使用プロセスと変更手順を表示       |
| VRChat状態不明                   | Discordを変更しない        |
| Discord状態不明                  | VRChatを変更しない         |
| VRChat操作確認失敗                 | 連動をDegradedへ変更       |
| Token Broker停止               | 有効なトークン期限内は継続        |
| 設定ファイル破損                     | バックアップ後に初期化          |
| Windows Credential Manager失敗 | 認証情報を保存せず警告          |
| スリープ復帰                       | 全接続を再初期化             |

---

# 23. テスト計画

## 23.1 単体テスト

* VRChatマイクオンからDiscordミュートを算出できる
* VRChatマイクオフからDiscordミュート解除を算出できる
* DiscordミュートからVRChatマイクオンを算出できる
* Discordミュート解除からVRChatマイクオフを算出できる
* VRChat優先モードでDiscord操作を無視できる
* `Unknown`状態でトグルを送信しない
* 同じ状態へ重複命令を送信しない
* 自己操作イベントを再処理しない
* 古いシーケンスを無視できる
* 同時操作で最後の外部操作を優先できる
* Discord RPCフレームを分割受信できる
* Discord RPCフレームを連続受信できる
* 不正JSONを安全に破棄できる
* 不正OSCを安全に破棄できる
* 設定ファイルを原子的に保存できる

## 23.2 結合テスト

* 疑似Discord Named Pipe
* 疑似VRChat OSC送受信
* Discord切断と再接続
* VRChatアバター変更
* Discord認可期限切れ
* Token Broker通信失敗
* DiscordとVRChatの同時操作
* Windowsスリープ・復帰
* 多重起動
* OSCポート競合
* Credential Manager読み書き

## 23.3 実環境テスト

* VRChat Desktopモード
* VRChat VRモード
* Discord Stable
* Discord PTB
* Discord Canary
* Windows 10
* Windows 11
* 複数アバター切り替え
* 複数OSCアプリ併用
* Discord音声設定変更アプリ併用
* Discord VC参加前後
* Discord再起動
* VRChat再起動
* PCスリープ復帰
* ネットワーク切断中の既存認証

---

# 24. 受け入れ条件

## 24.1 反転連動

* VRChatマイクオンでDiscordがミュートされる
* VRChatマイクオフでDiscordミュートが解除される
* DiscordミュートでVRChatマイクがオンになる
* Discordミュート解除でVRChatマイクがオフになる
* 同じ状態への命令が繰り返されない
* 無限反転が発生しない
* 状態不明時に推測操作しない

## 24.2 VRChat優先

* VRChat状態がDiscordへ反映される
* Discord操作でVRChatが変化しない
* Discord操作直後に状態を戻さない
* 次のVRChat状態変化でDiscordへ反映される
* 手動再同期でVRChat状態へ統一できる

## 24.3 Discord RPC

* Discord画面のミュート操作を検出できる
* Discord再起動後に自動再接続できる
* 保存済み認証情報で自動認証できる
* Client Secretがクライアントへ含まれない
* トークンが平文ファイルへ保存されない
* RPC切断時にVRChatを勝手に変更しない

## 24.4 GUI

* 状態が一画面で確認できる
* VRChatとDiscordの接続状態を区別できる
* 連動方向を視覚的に確認できる
* ウィンドウを閉じてもタスクトレイで継続する
* 一時停止と再同期へ2操作以内で到達できる
* 視覚効果を無効化できる
* ウィンドウ非表示時に常時アニメーションしない

## 24.5 軽量性

* アイドル時CPU使用率0.3%未満
* メモリ使用量50MB以下
* バックグラウンド時GPU使用率がほぼ0%
* 通常連動時に外部ネットワーク通信しない
* 管理者権限なしで動作する

---

# 25. 開発フェーズ

## フェーズ1：コア連動

* Cargoワークスペース作成
* 状態モデル
* Voice Coordinator
* VRChat OSC
* Discord RPCフレーム
* Discord状態取得・変更
* Mockテスト

## フェーズ2：Discord認可

* Discord Developer Application
* `AUTHORIZE`
* `AUTHENTICATE`
* Token Broker
* Credential Manager
* トークン更新
* 認可解除

## フェーズ3：GUI

* Slintテーマ
* ダッシュボード
* 設定画面
* 診断画面
* システムトレイ
* 状態アニメーション
* アクセシビリティ

## フェーズ4：安定化

* 再接続
* スリープ復帰
* 同時操作
* OSC設定診断
* エラーハンドリング
* パフォーマンス計測

## フェーズ5：公開準備

* Discord RPC承認申請
* プライバシーポリシー
* 利用規約
* コード署名
* インストーラー
* 自動ビルド
* リリーステスト

---

# 26. 最終決定

VoxShift 1.0は、次の構成で実装する。

```text
Rust / Edition 2024
Slint Native GUI
Tokio current-thread runtime
Discord RPC over Windows Named Pipe
VRChat OSC over UDP
Windows Credential Manager
Stateless Token Broker
Task Tray Resident Application
No VoxShift Account
No Database
No Telemetry
No Administrator Privileges
```

通常のマイク連動処理は完全にローカルで行う。

外部通信はDiscord初回認可、OAuth2トークン更新、認可解除に限定する。

GUIはVRChatとDiscordを左右の音声カードとして表示し、中央のリンクによって連動方向と現在の音声経路を分かりやすく表現する。常駐時の軽量性を優先し、アニメーションは状態変化時だけ実行する。

Discord RPCの承認を一般公開の必須条件とし、承認前は最大50名のテスターを対象としたクローズドテスト版として開発する。
